#!/usr/bin/env nextflow

params {
    // General
    outdir = './results'

    modules {
        'cellranger_filter_gtf' {
            publish_dir      = "cellranger/filter_gtf"
            publish_results  = "all"
            outfile = "filtered.gtf"
        }
    }
}